navigation_mapping:
  description: 建图阶段导航（仅激光）
  mode: mapping
  sensor_mode: laser_only
  default_strategy: coverage_patrol
  available_strategies:
    - coverage_patrol
    - wall_follow_coverage
  safety:
    obstacle_source_topic: /scan
    enable_dynamic_obstacle_avoidance: true
