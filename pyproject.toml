[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tj-robot-system"
version = "0.1.0"
description = "ROS 2 workspace-first repository. Runtime packages live in ros_ws/src."
requires-python = ">=3.10"
